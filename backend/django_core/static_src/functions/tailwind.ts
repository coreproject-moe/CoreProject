// Code from : https://stackoverflow.com/a/59982143
// We will address this in v4
import resolveConfig from "tailwindcss/resolveConfig";
import tailwindConfig from "$tailwind";

export default resolveConfig(tailwindConfig);
