[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Web server
fastapi = "*"
uvicorn = {extras = ["standard"], version = "*"}
# Data validation
pydantic = {extras = ["email"], version = "*"}
# JSON Enhancements
orjson = '*'
# Orm
tortoise_orm = {extras = ["asyncpg",'accel'], version = "*"}
aerich = "*"
# Caching
fastapi_cache2 = {extras = ['redis'], version = "*"}
# Templating
jinja2 = '*'
# Auth tools
python_jose = {extras = ["cryptography"], version = "*"}
passlib = {extras = ["argon2"], version = "*"}
# Redis
redis = ">=4"
hiredis = {version="*", markers="sys_platform != 'win32'"}
# HTTP Client
aiohttp = {version=">=3.8,<4", extras=['speedups']}
# HTTP Client Enhancements
aiohttp_retry = "*"
aiohttp_client_cache = {extras = ["redis"], version = "*"}
pyrate_limiter = {extras = ["redis"], version = "*"}

[dev-packages]
black = "*"
pylint = "*"
isort = "*"
# Flake8
flake8 = "*"
flake8-bugbear = '*'
flake8-logging-format="*"
# Mypy
mypy = "*"
types_redis = '*'
types_python_jose = '*'
types_cryptography = "*"


[requires]
python_version = "3.10"

[scripts]
dev = 'uvicorn main:app --reload'
init = 'aerich init-db'
makemigrations = 'aerich migrate'
migrate = 'aerich upgrade'

[pipenv]
allow_prereleases = true
